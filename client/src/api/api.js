export const host = "/api";

export const getLocation = `${host}/location`;

export const getProductsRoute = `${host}/products`;

export const getProductDetailsRoute = `${host}/productdetails`;

export const signInUser = `${host}/signinuser`;

export const placeOrderRoute = `${host}/placeorder`;
